require(["q", "squire"], function (Q, Squire) {

  describe("DataService", function () {

    describe("when getPizzas is called", function () {

      it("should return a promise resolving to an array of pizzas", function () {
        // var injector = new Squire();
        // injector.mock("pajamas", jasmine.createSpy("pjSpy").andReturn(Q([])));

        // injector.require(["dataService"], function (dataService) {
        //   expect(dataService.getPizzas()).toBeFalsy();
        // });
        expect(1).toEqual(1);
      });

    });

  });

});
